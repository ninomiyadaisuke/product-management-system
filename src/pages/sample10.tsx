import type { NextPage } from 'next';
import { Layout } from 'src/components/layout';

const sample10: NextPage = () => {
  return (
    <Layout pageTitle="top" title="sample10" subTitle="sample10">
      <div></div>
    </Layout>
  );
};

export default sample10;

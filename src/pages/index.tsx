import type { NextPage } from 'next';
import { Layout } from 'src/components/layout';

const Home: NextPage = () => {
  return (
    <Layout pageTitle="top" title="Product Add" subTitle="Create new product">
      <div></div>
    </Layout>
  );
};

export default Home;

import type { NextPage } from 'next';
import { Layout } from 'src/components/layout';

const sample2: NextPage = () => {
  return (
    <Layout linksTitle="Sales" pageTitle="top" title="sample2" subTitle="sample2">
      <div></div>
    </Layout>
  );
};

export default sample2;
